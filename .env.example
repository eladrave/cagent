# Cagent Environment Variables Example

# API Server Configuration
PORT=8090

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Examples:
#   Production: CORS_ALLOWED_ORIGINS=https://cagent-web-950783879036.us-central1.run.app,https://chat.eladrave.com
#   Development: CORS_ALLOWED_ORIGINS=http://localhost:8000,http://localhost:8080,http://localhost:3000
CORS_ALLOWED_ORIGINS=https://cagent-web-950783879036.us-central1.run.app,https://chat.eladrave.com,http://localhost:8000,http://localhost:8080

# Authentication
JWT_SECRET=your-secret-key-here

# Cloud Storage (for Cloud Run deployment)
GCS_BUCKET=your-bucket-name
GCP_PROJECT_ID=your-project-id

# AI Provider API Keys (at least one is required)
OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key
GROQ_API_KEY=your-groq-api-key

# Optional: Database Configuration
DATABASE_URL=sqlite:///work/sessions.db

# Optional: Disable authentication (for testing only)
# DISABLE_AUTH=true